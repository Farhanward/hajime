//! WhatsApp gateway.
//!
//! A WAHA-compatible API in front of a `whatsmeow` bridge. The API shape is
//! inherited so the existing workflows need no edit; the honesty is not.

pub mod api;
pub mod bridge;
